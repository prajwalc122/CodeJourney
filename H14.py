numbers = [5, 2, 9, 1]

print(len(numbers))    # show the  Length of an numbers.
print(max(numbers))    # Find the Max value
print(min(numbers))    # Find the Min value

numbers.sort()         # Sort ascending
numbers.reverse()      # Reverse list
