//def named function calls than gave function name called 
array sum than give s=0 because s value will be zero
than usinfg for loop to iteration give x variable name 
and arr ,
and s+=x means S=s+x so we 
x=10 adda with s and as its iterates finally using return keyword
to give result.



arr=[10,20,30,40,50]
def array_sum(arr):
    s = 0
    for x in arr:
        s += x
    return s
